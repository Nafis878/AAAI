# Day 2 worklog (timestamps local, 2026-07-18/19)

- **22:34** — Session start. Pre-flight: git clean @7984b16, pytest 15/15 green. Disk 7.2 GB free (97% full) → fp16 checkpoints mandatory.
- **22:35–22:45** — Checkpoint upgrade in `src/train.py`: ~35 log-spaced epochs (dense early), fp16 state_dicts under `experiments/<run>/checkpoints/`; `save_ckpt`/`load_ckpt`; early-stop epoch also checkpointed. `src/launch.py`: `--skip-existing` (idempotent relaunch keyed on `checkpoints/ckpt_final.pt`). `src/progress.py`: monitor (per-run status, aggregate ep/s vs 11 basis, disk). Tests: schedule density + fp16 round-trip → **17/17 green**. Dry-runs verified (core = 20 jobs).
- **22:36:41 — PASS A LAUNCHED** (detached Start-Process; `logs/passA.log`). 20 jobs = 5 PE × seeds 0–3, ≤15k epochs, 8 workers, `--skip-existing`. **Gate clocks: hour-2 pace gate at 00:37; hour-6 pivot gate at 04:37.**
- **22:39** — Verified: 8 workers alive, run dirs + log-spaced fp16 checkpoints appearing (nope/learned seeds 0–3 first). Disk 3.1 GB free (dropped 4 GB at launch — pagefile growth suspected; monitor armed at <1.5 GB threshold). Persistent monitor armed on passA.log (failures/completion/disk).
- **22:45–22:55** — Phase 2 started. scipy 1.18.0 installed + pinned (D11). `src/analysis.py` written: H1 onset/sharpness/censoring + Kruskal–Wallis/Mann–Whitney–Holm/k-group log-rank; H2 signatures (embed/logit Fourier concentration, logit-level restricted/excluded loss per D12, attention entropy, uniform-ablation delta) + label-permutation test; H3 LOO signature-vs-PE-label comparison. 7 new metric unit tests — **24/24 green** (one test fixture corrected: single-frequency clock logits have intrinsic near-ties; 5-freq fixture used, matching real grokked nets).
- **22:57** — DISK: dropped to 2.0 GB. Root cause found: pip HTTP cache = 3.4 GB. Purged → **5.0 GB free**; pagefile allocated 12.6 GB explains the launch-time 4 GB drop. No further risk expected; monitor stays armed. Note: run CSVs are write-buffered (~8 KB ≈ first ~600 epochs), so progress.py is blind to fresh runs for ~15 min — cosmetic, gates use later data.
- **22:50–22:58 — FALSE-ALARM incident (recorded for honesty; no experimental impact).** progress.py's CSV-only view + my misreading of wall-clock time produced an apparent "all workers frozen since epoch 1177" stall. Diagnostics run: per-process CPU sampling (all 8 workers at 100% of a core), py-spy stack dumps (normal forward passes, epochs ~1900), a denormal-float probe on the 1177 checkpoint (hypothesis REJECTED: 0 subnormals, FTZ speedup 1.0×). Resolution: no stall ever existed — checkpoints were exactly on schedule (1561 landed on time); pace is ~1.6 ep/s per worker ≈ **13 ep/s aggregate, ABOVE the 11 basis**. Fixes shipped: progress.py now falls back to checkpoint filenames for live epoch (source-aware freshness windows), py-spy added as a diagnostic tool (venv-only, not in requirements — dev tool). Lesson logged: never infer "stalled" from a single progress source; CSV buffering ≈ 2k-epoch blind spot.
- **23:00** — All 8 workers healthy: epochs 1561–2071 visible, val acc ~0.26–0.30 (pre-grok plateau, as expected), disk 9.8 GB free (pagefile shrank back). Aggregate ≥10.6 ep/s by the conservative logged-epoch count. Next: hour-1 pace preview at 23:40 (timer armed), hour-2 gate 00:37.
- **23:40 — HOUR-1 PACE PREVIEW.** 10.7 ep/s conservative (checkpoint-lagged count; true pace higher), epoch ~4.9–5.5k/15k, val acc still plateaued ~0.26–0.31, zero stderr, disk 10.1 GB. Worker private memory 810–936 MB each (~6.8 GB total) — stable vs 23:33 reading, no growth trend; watch continues. Preview verdict: comfortably above the 8.8 gate threshold; no action indicated. Formal gate at 00:37.

## 2026-07-19

- **00:48 — HOUR-2 PACE GATE: PASS (D13).** 11.7 ep/s ≥ 8.8. FIRST GROKS: nope_s1 early-stopped ep 7365 (onset ≈6.4k), learned_s3 ep 10405 (onset ≈9.4k). 6 first-batch runs still pre-grok at ~11.3k; sinusoidal s0/s1 started on freed workers. Censoring gate: 2/20 grokked, no censoring evidence yet — rolling watch continues at ~02:30 check. Disk 10.0 GB, memory stable.
- **(overnight)** — Session terminated after ~00:50; 02:30 and 04:37 wakeups never fired (D14). Detached Pass A ran to completion unaffected: **20/20 done in 6.74 h, 0 failures** (~05:21).
- **10:53 — Session resumed.** Pass A inventory: 5 early-stopped groks (onsets 6370/7130/8965/9410/13775), 6 more runs at val ≥0.99 at cap (sustained window incomplete → censored under strict rule), 9 genuinely not grokked — of which ALL FOUR sinusoidal (val 0.25–0.93). **10:54:26 — PASS B LAUNCHED** (10 OOD runs, detached, 8 workers verified alive).
- **10:56–11:10** — Pre-registered analyses on Pass A: H1 log-rank χ²=2.5 p=0.64 (15/20 censored — underpowered); H2 all-runs permutation **η²=0.434 p=0.013**; H2 matched-post-grok (pre-registered conditioning) η²=0.29 p=0.52 (n=11, underpowered). Signature table: grokked PEs have restricted_loss≈0 + excluded_loss 12–18 (full Fourier circuits); sinusoidal partial (restricted 1.5); **uniform-ablation delta separates learned (5.9) from nope/rope/alibi (1.3–2.3)** — candidate H2 headline, needs Pass-C power. Retroactive gates logged (D15): censoring gate NO ACTION (differential by PE), pivot gate NO PIVOT. Figs 1/2/4 + Table 1 regenerated from real data; H2 trajectory computation launched in background (logs/h2traj.log).
- **11:11–11:30** — Trajectories harvested (694 ckpt signatures): Fourier concentration rises at/just before each run's grok onset; sinusoidal never concentrates (final 0.33). Fig 3 written. Pass-C machinery built + tested: `extend40` preset (9 × 40k reruns as `_x40`, append-only; finished extensions supersede originals in analysis + figures). 26 tests green. Committed.
- **13:03 — PASS B MID-CHECK.** 8 runs at epoch ~11.3–12.2k/15k, pace ~1.45 ep/s/worker, zero failures. nope_s1_ood grokked (val 0.998, sustained window filling). Others still plateaued at 12k — OOD-holdout runs grok later than core runs (candidate H3-adjacent observation). Sinusoidal OOD tracking its core pattern (val 0.17–0.18). First 8 done ~13:40; ALiBi pair ~15:00–15:45.
- **13:18:41 — TRAINING PAUSED at user request** (D-series note: user-directed, not a gate action). Method: `scripts/pause_resume.ps1` — NtSuspendProcess on all 18 grid processes (launcher + 8 workers + venv stubs); verified 0 CPU burn after suspension. Runs frozen in memory at epoch ~12.5k (OOD); NO progress lost; resumable in place via `-Action resume`. Do NOT reboot the machine while paused — suspended processes do not survive reboot (would restart runs from epoch 0 via --skip-existing relaunch). All timers/ETAs shift by the pause duration. Awaiting user go-ahead to resume.
